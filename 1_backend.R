generate_data <- function(
    diff = 1,
    n = 10,
    s = 1000
) {
  #
  res <- matrix()
  # simulation 1st group
  # simulation 2nd group
  return(res)
}

plot_hist <- function(
    mat
) {
  # plot 1st histogram
  hist()
  # plot 2nd histogram
  hist(, add = TRUE)
}
